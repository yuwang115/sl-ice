# SL-ICE — 项目计划书

## 冰盖模拟器：一个基于 Blatter–Pattyn 应力模型的交互式网页教育产品

**作者：** Yu Wang
**日期：** 2026年3月
**版本：** v1.0

---

## 目录

1. [项目概述](#1-项目概述)
2. [设计哲学](#2-设计哲学)
3. [科学内核：Blatter–Pattyn Flowline 模型](#3-科学内核blatterpattyn-flowline-模型)
4. [游戏与交互设计](#4-游戏与交互设计)
5. [视觉设计规范](#5-视觉设计规范)
6. [技术架构](#6-技术架构)
7. [文件结构](#7-文件结构)
8. [开发里程碑](#8-开发里程碑)
9. [破圈与运营策略](#9-破圈与运营策略)
10. [附录：数学公式完整推导](#10-附录数学公式完整推导)

---

## 1. 项目概述

### 1.1 一句话描述

一个运行在浏览器中的实时冰盖物理模拟器，让任何人通过调节气候参数，亲手体验冰盖的生长、退缩、不可逆崩塌和海平面上升——基于真实的冰盖动力学方程（Blatter–Pattyn 近似）。

### 1.2 核心目标

- **教育目标：** 让公众和学生直觉理解 Marine Ice Sheet Instability (MISI)、tipping points、正反馈循环和不可逆性
- **传播目标：** 打造一个可分享、可嵌入课堂、能被媒体引用的开源交互产品
- **科学目标：** 在简化的同时保留核心物理——使用 Blatter–Pattyn 高阶近似而非 SIA+SSA 拼接，在接地线附近获得更准确的力学行为

### 1.3 目标用户

| 用户群体 | 使用场景 | 核心需求 |
|----------|---------|---------|
| 普通公众 | 社交媒体/新闻链接点入 | 30秒内理解操作，2分钟内获得"oh shit"时刻 |
| 中学生（14-18岁） | 地理/科学课堂 | 配合教案，直观理解气候变化后果 |
| 大学本科生 | 冰川学/气候学课程 | 理解 MISI、buttressing、滞后效应等概念 |
| 科学传播者/记者 | 嵌入文章或演讲 | 可截图、可嵌入的专业可视化 |

### 1.4 为什么选择 Blatter–Pattyn？

在教育场景中，接地线附近的行为是所有"戏剧性时刻"的来源。SIA 在接地线处失效（不含纵向应力），SSA 在内陆失效（不含垂直剪切），SIA+SSA 的拼接在过渡区不够物理。Blatter–Pattyn (BP) 作为 higher-order 近似，在单一框架中统一处理了内陆冰盖到冰架的完整过渡，无需人为拼接，接地线处的力学行为更加可靠。

对于一个要让用户"看到冰盖崩塌"的教育产品，这种物理保真度的提升直接影响用户体验的可信度。

---

## 2. 设计哲学

### 2.1 核心原则

1. **"玩" 在 "学" 之前：** 用户首先是玩家，其次才是学习者。任何教育内容都不应阻断交互流
2. **物理简化但不失真：** 可以降低分辨率、简化边界条件，但核心动力学机制（MISI、buttressing、冰下水润滑）必须定性正确
3. **30秒规则：** 用户在30秒内必须能看到自己的操作产生了可见的物理效果
4. **情感钩子优先：** 每个设计决策都要问——这是否会让用户想截图/分享？
5. **渐进式复杂度：** 沙盒→关卡→真实世界，每一层增加复杂度但保持同一个引擎

### 2.2 "Oh Shit" 时刻设计

整个产品围绕几个精心设计的情感高潮：

- **第一次崩塌：** 用户把温度调高，冰盖开始缓慢退缩，然后突然加速崩塌——MISI 的正反馈启动
- **回不去了：** 用户试图把温度调回去，但冰盖并没有恢复——滞后效应
- **冰架"瓶塞"拔出：** 用户融化冰架后，冰盖加速涌入海洋
- **冰下洪水：** 开启冰下水文后，水在冰底润滑基岩，冰流突然加速
- **你的城市被淹了：** 海平面数字对应真实城市——"你刚刚让上海被淹没了"

---

## 3. 科学内核：Blatter–Pattyn Flowline 模型

### 3.1 模型概述

本项目采用 2D (x-z) flowline 配置下的 Blatter–Pattyn (BP) 高阶近似。在一条从冰盖中心（ice divide）到大陆架前缘（calving front）的流线上，求解冰厚演化与速度场。

**坐标系：**
- x：水平方向，沿流线，从冰盖中心（x=0）到 calving front（x=L）
- z：垂直方向，从基岩（z=b(x)）到冰面（z=s(x) = b(x) + H(x)）
- 使用 terrain-following 坐标 σ = (z - b) / H，σ ∈ [0, 1]

### 3.2 控制方程

#### 3.2.1 冰厚演化方程（质量守恒）

```
∂H/∂t = ȧ - ḃ - ∂(ūH)/∂x
```

其中：
- H(x,t)：冰厚
- ȧ(x,t)：表面质量平衡（降雪 - 表面融化），由大气温度参数化
- ḃ(x,t)：底部融化率（对冰架底部由海洋温度参数化，对 grounded ice 由地热通量和摩擦生热决定）
- ū(x,t)：深度平均水平速度

#### 3.2.2 Blatter–Pattyn 动量方程

在 flowline (x-z) 配置下，BP 近似的水平动量方程为：

```
∂/∂x(4η ∂u/∂x) + ∂/∂z(η ∂u/∂z) = ρᵢg ∂s/∂x
```

其中：
- u(x,z)：水平速度
- η：有效黏度（非线性，依赖应变率）
- ρᵢ：冰密度（917 kg/m³）
- g：重力加速度（9.81 m/s²）
- s(x)：冰面高程

**有效黏度：**
```
η = ½ A⁻¹/ⁿ · ε̇ₑ^((1-n)/n)
```

其中：
- A(T)：流动律速率因子（Glen's flow law），温度依赖，使用 Arrhenius 关系
- n = 3：Glen's flow law 指数
- ε̇ₑ：有效应变率

**有效应变率（flowline BP）：**
```
ε̇ₑ² = (∂u/∂x)² + ¼(∂u/∂z)² + ε̇₀²
```

其中 ε̇₀ 是小的正则化参数（~10⁻¹⁰ s⁻¹），防止零应变率时黏度发散。

#### 3.2.3 边界条件

**冰面（z = s）— 无应力：**
```
∂u/∂z|_(z=s) = 0     （忽略大气应力）
```

**冰底（z = b）— Grounded Ice 滑动律：**
```
τ_b = C · u_b^m · N^q
```

其中：
- τ_b：底部剪应力
- C：滑动系数
- u_b：底部滑动速度
- m = 1/3（非线性滑动指数）
- N：有效压力（冰盖静水压力 - 冰下水压力）
- q = 1（有效压力指数）

**冰底（z = b）— Floating Ice（冰架）：**
```
τ_b = 0     （冰架底部自由滑动）
```

**冰盖中心（x = 0）— 对称：**
```
u(0, z) = 0
∂H/∂x|_(x=0) = 0
```

**Calving Front（x = L）— 海洋压力：**
```
2η(∂u/∂x)|_(x=L) · H = ½ ρᵢg H² - ½ ρ_w g D²
```

其中 D = min(H · ρᵢ/ρ_w, water depth) 是吃水深度。

#### 3.2.4 接地线处理

接地线位置 x_gl 由浮力条件确定：

```
ρᵢ H(x_gl) = -ρ_w b(x_gl)
```

在接地线处，模型需要在 grounded（有底部摩擦）和 floating（无底部摩擦）之间平滑过渡。实现方式为在接地线附近的网格单元中，根据浮力条件对底部摩擦做线性插值（sub-element parameterization），确保力学连续性。

### 3.3 参数化方案

#### 3.3.1 表面质量平衡（SMB）

采用简化的温度-高程参数化：

```
ȧ(x) = min(a_max, a_0 + λ_a · (T_ref - T_atm + Γ · s(x)))
```

其中：
- a_max：最大积累率上限（~0.5 m/yr ice eq.）
- a_0：参考积累率
- λ_a：温度敏感性系数
- T_atm：**用户可控**的大气温度参数
- Γ：温度递减率（~6.5 °C/km）
- s(x)：冰面高程

同时引入消融项（ablation）：
```
当 T_local > 0°C 时: ablation = d_melt · max(0, T_local)
```

即正度日模型（PDD）的简化版。

#### 3.3.2 冰架底部融化（Ocean Melting）

采用二次温度依赖参数化：

```
ḃ_shelf(x) = γ_T · (T_ocean - T_f)² · F(depth)
```

其中：
- γ_T：热交换系数
- T_ocean：**用户可控**的海洋温度参数
- T_f：压力依赖的冰点温度（~-1.9°C + 压力修正）
- F(depth)：深度依赖的缩放因子

#### 3.3.3 冰下水文（Subglacial Hydrology）

采用简化的稳态水膜模型，计算有效压力 N：

```
N(x) = ρᵢ g H(x) · (1 - k_w · W(x))
```

其中 W(x) 是归一化的冰下水量（0 = 干燥，1 = 完全浸没），由冰底融化率和排水效率决定：

```
∂W/∂t = Q_melt / (ρ_w · L_f) - W / τ_drain + ∂/∂x(K_w · W · ∂ψ/∂x)
```

其中：
- Q_melt：底部融化产生的水量（地热 + 摩擦生热）
- τ_drain：排水时间尺度（**用户可控**）
- K_w：水力传导率
- ψ：水力梯度

**用户控制参数：** 排水效率（τ_drain），对应"冰下水文系统的效率"——低排水效率导致高水压，低有效压力，快速滑动。

#### 3.3.4 温度场（简化）

冰的流动律速率因子 A 依赖温度。为降低计算量，不完整求解热方程，而是采用解析/参数化的温度剖面：

```
T(σ) = T_s + (T_b - T_s) · f(σ, Pe)
```

其中 f 是基于 Peclet 数（Pe = 积累率 × 冰厚 / 热扩散率）的解析函数。T_s 为冰面温度（与 T_atm 相关），T_b 为冰底温度（受地热和摩擦生热影响，上限为压力融点）。

### 3.4 数值方法

#### 3.4.1 空间离散

**水平方向（x）：**
- 有限差分法（FDM）
- 均匀网格，50–100 个节点（域长度 ~800–1500 km）
- 水平分辨率 Δx ≈ 10–20 km

**垂直方向（σ）：**
- Terrain-following σ 坐标
- 10–20 个垂直层（可在底部加密）
- Δσ 均匀或不均匀

**总自由度：** 50×15 = 750 ~ 100×20 = 2000 个速度未知量。这对浏览器完全可行。

#### 3.4.2 Blatter–Pattyn 速度求解器

BP 动量方程是非线性椭圆方程（由于黏度依赖速度）。采用 **Picard 迭代**：

```
1. 用上一时间步（或初始猜测）的速度场计算黏度 η
2. 在冻结的 η 下，BP 方程变为线性椭圆方程
3. 将线性方程组装为矩阵 A·u = b
4. 求解线性系统（带状矩阵，直接求解或迭代求解）
5. 用新速度更新 η
6. 重复 2-5，直到收敛（||u_new - u_old|| / ||u_old|| < tol）
```

**矩阵结构：** 在 (x, σ) 网格上，5 点 stencil → 带状矩阵（bandwidth = N_σ）。可用 Thomas 算法的扩展或 LU 分解求解。

**收敛性：** 通常 5–15 次 Picard 迭代即可收敛。可设置最大迭代次数（如 20）作为安全阀。

#### 3.4.3 时间积分

冰厚演化方程采用**半隐式**时间步进：

```
(H^{n+1} - H^n) / Δt = ȧ^n - ḃ^n - ∂(ū^{n+1} · H^n) / ∂x
```

即扩散项（速度×冰厚的通量）使用新时间步的速度但旧时间步的冰厚。这允许较大的时间步而不引起数值不稳定。

**时间步长：** 以"模拟年"为单位，Δt = 1–10 年/步。实时模拟中，1 帧 ≈ 1–10 模拟年，用户可调节模拟速度。

#### 3.4.4 接地线追踪

接地线位置通过以下步骤在每个时间步更新：
1. 在每个水平节点检查浮力条件：ρᵢH > -ρ_w b → grounded，否则 floating
2. 接地线位于最后一个 grounded 节点和第一个 floating 节点之间
3. 线性插值确定精确位置
4. 在接地线附近的"过渡区"单元中，底部摩擦按浮力比例线性插值

### 3.5 基岩地形（预设情景）

#### 3.5.1 教学情景（合成地形）

**Scenario A "稳定冰盖"：**
基岩向海洋方向单调下降（prograde slope）。冰盖在扰动后趋于恢复。
```
b(x) = -100 - 500 · (x/L) - 200 · sin(πx/L)
```

**Scenario B "MISI 陷阱"：**
包含一个关键的 retrograde slope（基岩在接地线后方向内陆下降），这是触发 MISI 的地形条件。
```
b(x) = -200 - 400 · (x/L) + 300 · exp(-((x - 0.6L)/0.1L)²)
```
一个 overdeepening 后跟一个 retrograde slope。

**Scenario C "冰架峡谷"：**
有明显的冰架区域，侧向约束（buttressing）可参数化。
```
b(x) = -300 - 600 · (x/L) + 150 · sin(2πx/L)
```

#### 3.5.2 真实世界情景（简化真实数据）

基于 BedMachine 数据，提取以下区域的代表性 flowline 地形：

- **Wilkes Subglacial Basin (东南极)：** 你的主要研究区域，包含深层 overdeepening 和 retrograde slope
- **Thwaites Glacier (西南极)：** "末日冰川"，公众关注度最高
- **Totten Glacier (东南极)：** 含大量冰下水的深层冰盖

每条 flowline 以 JSON 数组形式存储基岩高程、冰面高程和初始冰厚。

### 3.6 物理参数表

| 参数 | 符号 | 默认值 | 单位 | 是否用户可控 |
|------|------|--------|------|-------------|
| 冰密度 | ρᵢ | 917 | kg/m³ | 否 |
| 海水密度 | ρ_w | 1028 | kg/m³ | 否 |
| 重力加速度 | g | 9.81 | m/s² | 否 |
| Glen 指数 | n | 3 | — | 否 |
| 流动律速率因子 | A | 1.0×10⁻¹⁶ | Pa⁻³ yr⁻¹ | 否（由温度自动计算） |
| 滑动系数 | C | 7.6×10⁶ | Pa m⁻¹/³ s¹/³ | 否 |
| 滑动指数 | m | 1/3 | — | 否 |
| 有效压力指数 | q | 1 | — | 否 |
| 大气温度 | T_atm | -20 | °C | **是**（核心控制） |
| 降雪率 | P_snow | 0.3 | m/yr | **是** |
| 海洋温度 | T_ocean | -1.5 | °C | **是**（核心控制） |
| 排水效率 | τ_drain | 1.0 | yr | **是**（水文关卡） |
| 地热通量 | Q_geo | 0.05 | W/m² | 否 |
| 正则化应变率 | ε̇₀ | 10⁻¹⁰ | s⁻¹ | 否 |

---

## 4. 游戏与交互设计

### 4.1 三层体验架构

```
┌─────────────────────────────────────────────────────────┐
│                         SL-ICE                           │
├──────────────┬──────────────────┬────────────────────────┤
│   SANDBOX    │   CHALLENGES     │   REAL WORLD           │
│   沙盒模式    │   挑战关卡        │   真实世界              │
│              │                  │                        │
│  自由探索     │  6+1 个教学关卡   │  Wilkes / Thwaites /   │
│  无目标       │  每个聚焦一个概念  │  Totten 真实地形        │
│  建立直觉     │  有胜负条件       │  科学故事 + 模拟        │
└──────────────┴──────────────────┴────────────────────────┘
```

### 4.2 Sandbox 沙盒模式

**界面布局：**
```
┌──────────────────────────────────────────────────────────────┐
│  [≡ 菜单]    SL-ICE                            [⏸ 暂停] [⏩] │
├──────────────────────────────────────────────────────┬───────┤
│                                                      │ 控制  │
│                                                      │ 面板  │
│            ☁️ 降雪动画                                │       │
│     ❄️  ❄️  ❄️  ❄️  ❄️  ❄️  ❄️                      │ 🌡️大气 │
│  ┌─────────────────────────────┐                     │ 温度  │
│  │         ICE SHEET           │  ← 冰面             │ [-20] │
│  │    (半透明蓝白渐变)          │                     │  ──── │
│  │  · · ·水流粒子· · · ·       │  ← 冰下水           │ 🌊海洋 │
│  ├─────────────────────────────┤                     │ 温度  │
│  │▓▓▓▓▓▓ BEDROCK ▓▓▓▓▓▓▓▓▓▓▓▓│  ← 基岩             │ [-1.5]│
│  │▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│                     │  ──── │
│  └─────────────────────────────┘                     │ ❄️降雪 │
│               ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒ ← 海洋             │ [0.3] │
│                    🔺 接地线标记                       │  ──── │
├──────────────────────────────────────────────────────┤ 💧排水 │
│  📅 Year: 2026   📊 Volume: 24.5 M km³              │ 效率   │
│  🌊 Sea Level: +0.00 m   ⚡ GL Velocity: 120 m/yr    │ [1.0] │
└──────────────────────────────────────────────────────┴───────┘
```

**用户可控制的参数（滑块）：**

| 滑块 | 范围 | 默认值 | 效果 |
|------|------|--------|------|
| 大气温度 ΔT | -5 ~ +10 °C（相对于当前） | 0 | 影响 SMB（降雪 vs 消融） |
| 海洋温度 ΔT_ocean | -2 ~ +5 °C（相对于当前） | 0 | 影响冰架底部融化 |
| 降雪倍率 | 0.5x ~ 2.0x | 1.0x | 缩放积累率 |
| 冰下排水效率 | 高效 ↔ 低效 | 中等 | 影响有效压力和滑动速度 |

**实时显示的状态量：**
- 当前模拟年份
- 冰盖总体积（km³）
- 等效海平面变化（m）—— 带城市淹没提示
- 接地线位置（km from divide）
- 接地线处速度（m/yr）

### 4.3 Challenges 挑战关卡

#### 关卡 1："稳态之道" (The Balance)

- **概念：** 冰盖的质量守恒——输入（降雪）= 输出（融化 + calving）
- **地形：** Scenario A（稳定的 prograde slope）
- **目标：** 调节温度和降雪，使冰盖体积在 500 模拟年内波动不超过 ±5%
- **教学点：** 冰盖是一个动态平衡系统
- **难度：** ★☆☆☆☆

#### 关卡 2："不归之路" (Point of No Return)

- **概念：** Marine Ice Sheet Instability (MISI)
- **地形：** Scenario B（含 retrograde slope 的 overdeepening）
- **目标：** 尝试阻止接地线退过 overdeepening。用户一开始不知道临界点在哪——需要试探
- **机制：** 接地线一旦退到 retrograde slope 上，正反馈启动，即使温度恢复也无法阻止
- **教学点：** 弹出解释：为什么在向内陆倾斜的基岩上，冰盖退缩会自我加速（冰厚增大 → 通量增大 → 更多退缩）
- **胜利条件：** 在 1000 年内接地线不退过标记线
- **失败后：** 展示滞后效应——把所有参数调回来，冰盖仍然无法恢复
- **难度：** ★★★☆☆

#### 关卡 3："瓶塞效应" (The Cork)

- **概念：** 冰架 buttressing
- **地形：** Scenario C（有明显冰架区域）
- **目标：** 用户只能控制海洋温度。维持冰架存在
- **机制：** 海洋温度升高 → 冰架底部融化 → 冰架变薄/消失 → buttressing 减弱 → 冰盖加速 → 海平面上升
- **教学点：** 冰架本身贡献的海平面上升很小（已经浮着），但它是阻挡冰盖的"瓶塞"
- **分数：** 按冰架存活时长和最终海平面上升量评分
- **难度：** ★★☆☆☆

#### 关卡 4："冰下暗流" (Undercurrents)

- **概念：** 冰下水文的正反馈
- **地形：** Scenario B（含 overdeepening）
- **特殊机制：** 本关开启冰下水文模块。用户控制排水效率
- **目标：** 理解冰下水如何影响冰盖滑动
- **机制：** 低排水效率 → 高水压 → 低有效压力 → 快速滑动 → 更多摩擦生热 → 更多底部融化 → 更多水 → 正反馈
- **教学点：** 连接到你的研究——东南极冰下水文如何加速不可逆冰损失
- **难度：** ★★★☆☆

#### 关卡 5："拯救冰盖" (Save the Ice)

- **概念：** 不可逆性与滞后效应 (Hysteresis)
- **地形：** Scenario B，初始状态接近 tipping point
- **目标：** 冰盖已经在临界边缘，用户必须找到正确的参数组合来"拯救"它
- **结局分支：**
  - 成功（极难）：找到降温+增雪的精确组合，勉强守住
  - 失败（大多数人的经历）：体验不可逆崩塌，然后看到即使完全恢复参数也无法逆转
- **教学点：** 在气候系统中，预防远比补救容易
- **难度：** ★★★★☆

#### 关卡 6："地球工程师" (The Geoengineers)

- **概念：** Glacial climate intervention
- **地形：** Thwaites 简化地形
- **特殊机制：** 用户可以在接地线前方"建造"海底屏障（curtain），部分阻挡暖水入侵
- **目标：** 用最小的干预，延缓冰盖崩塌 500 年
- **教学点：** 介绍冰川干预的前沿研究，同时让用户体会这有多难（部分有效，但不是万能药）
- **难度：** ★★★★☆

#### 隐藏关卡："蝴蝶效应" (The Butterfly)

- **解锁条件：** 完成前 6 关
- **概念：** 初始条件敏感性与不确定性
- **机制：** 给两个几乎相同（差 0.01°C）的初始条件，让用户同时运行对比，观察长期结果的巨大分歧
- **教学点：** 为什么冰盖预测存在不确定性，为什么 ensemble 模拟如此重要

### 4.4 Real World 真实世界模式

**选择界面：**
用户从一个南极洲俯视地图中选择区域（Wilkes / Thwaites / Totten），点击后加载该区域的真实 flowline 地形数据，进入模拟。

**每个区域包含：**
1. **科学背景卡片：** 2-3 段文字 + 关键数字（冰量、等效海平面、当前退缩速率）
2. **当前状态模拟：** 加载近似当前冰盖构型，运行 "business as usual" 情景
3. **What If 模式：** 用户可修改参数，探索不同气候路径
4. **研究亮点：** 链接到相关论文（包括你的）

### 4.5 社交分享功能

**模拟结束时生成"成绩单"：**

```
┌──────────────────────────────────────────┐
│             🧊 SL-ICE 🧊                 │
│                                          │
│   你模拟了 [Wilkes Subglacial Basin]      │
│                                          │
│   ⏱️ 模拟时长：500 年                     │
│   📉 冰量变化：-42%                       │
│   🌊 海平面上升：+1.8 m                   │
│   🏙️ 影响：上海、东京、迈阿密将被淹没      │
│                                          │
│   [🔗 分享] [🔄 再试一次] [📚 了解更多]    │
└──────────────────────────────────────────┘
```

分享按钮生成带预览图的链接（Open Graph meta tags），可一键分享到 Twitter/X、微博、LinkedIn。

---

## 5. 视觉设计规范

### 5.1 整体美学

**风格关键词：** 优雅示意图 · 科学插画感 · 克制而非写实

参考对象：
- [Bartosz Ciechanowski](https://ciechanow.ski/) 的交互式物理博客
- [The Pudding](https://pudding.cool/) 的数据故事
- [Neal.fun](https://neal.fun/) 的趣味交互
- [Earth.fm](https://earth.fm/) 的自然美学

### 5.2 配色方案

```
背景：    #0A1628 (深蓝夜空)
冰面：    #E8F4FD → #B8D8EA (白到浅蓝渐变，半透明)
冰体内部：#6FB3D2 → #2E7BA5 (浅蓝到深蓝，随深度加深)
冰架：    #A8D8EA (带透明度，与 grounded ice 区分)
基岩：    #3D2B1F → #5C4033 (深棕色，带纹理)
海洋：    #1A3A5C → #0D2137 (深蓝，带微妙波动)
暖流：    #FF6B35 → #FF4444 (暖橙到红，表示暖水入侵)
冰下水：  #4ECDC4 (青绿色，发光粒子)
接地线：  #FFD700 (金色标记，醒目)
危险/警告：#FF4444 (红色脉冲，冰盖崩塌时)
UI 文字：  #E0E0E0 (浅灰)
UI 强调：  #4ECDC4 (青绿)
```

### 5.3 动画效果

| 元素 | 动画 | 触发条件 |
|------|------|---------|
| 降雪 | 粒子从顶部飘落并堆积在冰面 | 始终（降雪率>0时） |
| 冰流 | 冰体内部的缓慢流线动画 | 始终 |
| 冰下水 | 发光粒子沿冰底流动 | 冰下水文开启时 |
| 暖水入侵 | 橙红色流体从海洋侧渗入冰架底部 | 海洋温度升高时 |
| 接地线退缩 | 金色标记平滑移动 + 脉冲效果 | 接地线位置变化时 |
| 冰山崩解 | 冰架前端碎片脱落粒子效果 | calving 发生时 |
| 冰盖崩塌 | 画面震动 + 红色脉冲 + 碎裂效果 | 冰盖快速崩塌时 |
| 海平面上升 | 海洋水位可见上升 | 冰盖质量减少时 |

### 5.4 信息设计

**动态注释气泡：** 当关键事件发生时（接地线退缩加速、冰架消失、MISI 启动），在画面中弹出简洁的解释性气泡。例如：

```
💡 接地线正在 retrograde slope 上退缩。
   基岩越深 → 冰越厚 → 冰流越快 → 退缩越快。
   这就是 Marine Ice Sheet Instability。
```

**实时仪表盘：** 屏幕底部或侧边的数据条，显示冰盖体积、海平面、速度等，使用迷你折线图展示趋势。

---

## 6. 技术架构

### 6.1 总体架构

```
┌─────────────────────────────────────────────────────┐
│                      Frontend                       │
│  React + TypeScript                                 │
│                                                     │
│  ┌──────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │ UI Layer │  │ Render Layer │  │  State Mgmt   │  │
│  │ (React)  │  │ (Canvas/     │  │  (Zustand)    │  │
│  │          │  │  PixiJS)     │  │               │  │
│  └────┬─────┘  └──────┬───────┘  └───────┬───────┘  │
│       │               │                  │          │
│       └───────────────┼──────────────────┘          │
│                       │                             │
│              ┌────────┴────────┐                    │
│              │  Physics Engine │                    │
│              │  (Web Worker)   │                    │
│              │                 │                    │
│              │  BP Solver      │                    │
│              │  Ice Evolution  │                    │
│              │  Hydrology      │                    │
│              │  GL Tracking    │                    │
│              └─────────────────┘                    │
└─────────────────────────────────────────────────────┘
```

### 6.2 核心技术选型

| 层 | 技术 | 理由 |
|----|------|------|
| 框架 | React 18+ + TypeScript | 组件化 UI、类型安全 |
| 状态管理 | Zustand | 轻量、适合游戏循环状态 |
| 2D 渲染 | HTML5 Canvas（原生）或 PixiJS | 性能好，粒子效果丰富 |
| 物理引擎 | TypeScript / Web Worker | 不阻塞 UI 线程 |
| 线性代数 | 手写带状矩阵求解器 或 ml-matrix 库 | BP 求解的核心 |
| 构建工具 | Vite | 快速 HMR，TypeScript 原生支持 |
| 部署 | Vercel / GitHub Pages | 零成本，自动 CI/CD |
| 样式 | Tailwind CSS 4 | 快速 UI 开发 |
| 国际化 | i18next | 英文为主，中文为辅 |

### 6.3 物理引擎（Web Worker）

物理引擎运行在 Web Worker 中，与 UI 线程分离。通信协议如下：

**主线程 → Worker：**
```typescript
interface PhysicsCommand {
  type: 'init' | 'step' | 'update_params' | 'reset';
  payload: {
    scenario?: ScenarioConfig;     // init 时传入
    params?: UserParams;           // update_params 时传入
    dt?: number;                   // step 时传入（模拟时间步长）
    nSteps?: number;               // 每帧计算的步数
  };
}

interface UserParams {
  T_atm_delta: number;      // 大气温度偏移 (°C)
  T_ocean_delta: number;    // 海洋温度偏移 (°C)
  precip_scale: number;     // 降雪倍率
  drain_efficiency: number; // 排水效率
  enable_hydrology: boolean;
  curtain_position?: number; // 地球工程关卡：海底屏障位置
  curtain_efficiency?: number;
}
```

**Worker → 主线程：**
```typescript
interface PhysicsState {
  type: 'state_update';
  payload: {
    year: number;
    H: Float64Array;         // 冰厚 profile
    u_surface: Float64Array; // 冰面速度 profile
    u_base: Float64Array;    // 冰底速度 profile
    s: Float64Array;         // 冰面高程 profile
    b: Float64Array;         // 基岩高程 profile
    gl_position: number;     // 接地线位置 (km)
    gl_velocity: number;     // 接地线处速度 (m/yr)
    volume: number;          // 冰盖体积 (km³)
    sea_level: number;       // 等效海平面变化 (m)
    shelf_exists: boolean;   // 冰架是否存在
    water_pressure: Float64Array; // 冰下水压 profile
    is_misi_active: boolean; // MISI 是否在进行中
    events: GameEvent[];     // 触发的游戏事件
  };
}

interface GameEvent {
  type: 'gl_retreat_accelerating' | 'shelf_collapsed' |
        'misi_triggered' | 'hysteresis_locked' |
        'city_flooded' | 'ice_recovered';
  message_en: string;
  message_zh: string;
  severity: 'info' | 'warning' | 'critical';
}
```

### 6.4 游戏循环

```
┌─────────────────────────────────────────────┐
│              GAME LOOP (~60 FPS)             │
│                                             │
│  1. 读取用户输入（滑块值）                    │
│  2. 发送参数到 Worker                        │
│  3. Worker 计算 N 步物理（N=1~10，自适应）    │
│  4. Worker 返回新状态                        │
│  5. 更新 Zustand store                      │
│  6. React/Canvas 渲染新帧                    │
│  7. 检测并显示游戏事件                        │
│  8. 更新统计仪表盘                           │
│                                             │
│  目标：物理计算 < 10ms/帧                    │
│        渲染 < 6ms/帧                         │
│        总计 < 16ms/帧 (60fps)               │
└─────────────────────────────────────────────┘
```

### 6.5 性能预算

| 操作 | 预算 | 策略 |
|------|------|------|
| BP 求解（1步） | < 5ms | 50×15 网格，Picard 5-10 迭代，带状矩阵直接求解 |
| 冰厚更新 | < 0.5ms | 1D FDM，50-100 节点 |
| 水文求解 | < 1ms | 简化稳态模型 |
| 渲染 | < 6ms | Canvas/PixiJS，仅更新变化区域 |
| 粒子系统 | < 2ms | 降雪/水流/崩解，限制粒子总数 ~500 |

**降级策略：** 如果在低端设备上帧率低于 30fps：
1. 减少垂直层数（20 → 10）
2. 减少粒子数
3. 降低渲染分辨率
4. 减少 Picard 迭代次数

### 6.6 移动端适配

- 响应式布局：控制面板在移动端折叠到底部
- 触控适配：滑块用 touch-friendly 的大尺寸
- 性能：移动端默认降低网格分辨率
- 横屏提示：推荐横屏使用

---

## 7. 文件结构

```
sl-ice/
├── README.md                           # 项目说明（英文）
├── README.zh.md                        # 项目说明（中文）
├── LICENSE                             # MIT License
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
│
├── public/
│   ├── favicon.ico
│   ├── og-image.png                    # 社交分享预览图
│   └── data/
│       ├── scenarios/
│       │   ├── scenario_a_stable.json  # 合成地形 A
│       │   ├── scenario_b_misi.json    # 合成地形 B
│       │   └── scenario_c_shelf.json   # 合成地形 C
│       └── real_world/
│           ├── wilkes_flowline.json    # Wilkes 真实地形
│           ├── thwaites_flowline.json  # Thwaites 真实地形
│           └── totten_flowline.json    # Totten 真实地形
│
├── src/
│   ├── main.tsx                        # 入口
│   ├── App.tsx                         # 主应用组件
│   │
│   ├── engine/                         # 物理引擎（运行在 Web Worker 中）
│   │   ├── worker.ts                   # Web Worker 入口
│   │   ├── physics.ts                  # 物理主循环
│   │   ├── bp-solver.ts               # Blatter–Pattyn 速度求解器
│   │   ├── ice-thickness.ts           # 冰厚演化方程
│   │   ├── hydrology.ts              # 冰下水文模块
│   │   ├── grounding-line.ts          # 接地线追踪
│   │   ├── smb.ts                     # 表面质量平衡参数化
│   │   ├── ocean-melt.ts             # 冰架底部融化
│   │   ├── temperature.ts            # 冰温参数化
│   │   ├── constants.ts              # 物理常数
│   │   ├── types.ts                  # 类型定义
│   │   └── matrix/
│   │       ├── banded.ts             # 带状矩阵求解器
│   │       └── utils.ts              # 矩阵工具函数
│   │
│   ├── store/                         # 状态管理
│   │   ├── game-store.ts             # 游戏状态（模式、关卡、得分）
│   │   ├── physics-store.ts          # 物理状态（从 Worker 接收）
│   │   └── ui-store.ts               # UI 状态（面板开关、语言）
│   │
│   ├── renderer/                      # 渲染层
│   │   ├── IceSheetCanvas.tsx         # 主 Canvas 组件
│   │   ├── draw-ice.ts               # 冰盖绘制
│   │   ├── draw-bedrock.ts           # 基岩绘制
│   │   ├── draw-ocean.ts             # 海洋绘制
│   │   ├── draw-annotations.ts       # 标注和动态气泡
│   │   ├── particles/
│   │   │   ├── snowfall.ts           # 降雪粒子
│   │   │   ├── water-flow.ts         # 冰下水流粒子
│   │   │   ├── ocean-current.ts      # 海洋暖流粒子
│   │   │   └── calving.ts            # 冰山崩解粒子
│   │   └── effects/
│   │       ├── screen-shake.ts       # 画面震动
│   │       └── pulse-glow.ts         # 脉冲发光效果
│   │
│   ├── components/                    # UI 组件
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx            # 侧边控制面板
│   │   │   └── Dashboard.tsx          # 底部仪表盘
│   │   ├── controls/
│   │   │   ├── ParameterSlider.tsx    # 参数滑块
│   │   │   ├── PlaybackControls.tsx   # 播放/暂停/快进
│   │   │   └── SpeedControl.tsx       # 模拟速度
│   │   ├── game/
│   │   │   ├── ModeSelector.tsx       # 模式选择（沙盒/关卡/真实）
│   │   │   ├── ChallengePanel.tsx     # 关卡说明和目标
│   │   │   ├── ScoreCard.tsx          # 成绩单
│   │   │   └── ShareCard.tsx          # 社交分享卡
│   │   ├── education/
│   │   │   ├── InfoBubble.tsx         # 动态解释气泡
│   │   │   ├── ConceptCard.tsx        # 概念卡片
│   │   │   └── GlossaryModal.tsx      # 术语表
│   │   └── real-world/
│   │       ├── AntarcticaMap.tsx       # 南极选区地图
│   │       └── RegionInfo.tsx         # 区域科学背景
│   │
│   ├── challenges/                    # 关卡定义
│   │   ├── index.ts                   # 关卡注册
│   │   ├── types.ts                   # 关卡类型定义
│   │   ├── challenge-1-balance.ts
│   │   ├── challenge-2-misi.ts
│   │   ├── challenge-3-buttressing.ts
│   │   ├── challenge-4-hydrology.ts
│   │   ├── challenge-5-save.ts
│   │   ├── challenge-6-geoeng.ts
│   │   └── challenge-7-butterfly.ts
│   │
│   ├── i18n/                          # 国际化
│   │   ├── en.json
│   │   └── zh.json
│   │
│   ├── utils/
│   │   ├── sea-level-impacts.ts       # 海平面→城市影响映射
│   │   └── share.ts                   # 社交分享工具
│   │
│   └── styles/
│       └── globals.css                # 全局样式 + CSS 变量
│
└── tests/
    ├── engine/
    │   ├── bp-solver.test.ts          # ISMIP-HOM 基准测试
    │   ├── ice-thickness.test.ts
    │   └── grounding-line.test.ts     # MISMIP 基准测试
    └── challenges/
        └── misi-trigger.test.ts       # 确保 MISI 可被触发
```

---

## 8. 开发里程碑

### Phase 0：物理引擎原型（2-3 周）

**目标：** 在浏览器中运行 BP flowline 模型，无 UI，纯控制台输出。

**任务：**
- [ ] 搭建项目框架（Vite + React + TypeScript）
- [ ] 实现带状矩阵求解器 (`matrix/banded.ts`)
- [ ] 实现 BP 速度求解器 (`bp-solver.ts`)——Picard 迭代
- [ ] 实现冰厚演化方程 (`ice-thickness.ts`)——半隐式时间步进
- [ ] 实现接地线追踪 (`grounding-line.ts`)
- [ ] 实现 SMB 参数化 (`smb.ts`)
- [ ] **验证：** 用 ISMIP-HOM Experiment A/B 验证 BP 求解器
- [ ] **验证：** 用 MISMIP 实验验证接地线行为
- [ ] 将物理引擎移入 Web Worker

**验收标准：** BP 求解器通过 ISMIP-HOM 基准；接地线在 MISMIP 地形上表现正确（prograde stable, retrograde unstable）；单步计算 < 10ms。

### Phase 1：最小可玩产品（2-3 周）

**目标：** Sandbox 模式可玩——用户拖动滑块，冰盖实时响应。

**任务：**
- [ ] Canvas 渲染器：冰盖、基岩、海洋的基本绘制
- [ ] 参数滑块 UI（温度、降雪、海洋温度）
- [ ] 游戏循环：滑块 → Worker → 状态 → 渲染
- [ ] 实时仪表盘（年份、体积、海平面）
- [ ] 播放/暂停/速度控制
- [ ] 接地线标记

**验收标准：** 60fps 流畅运行；调高温度可以看到冰盖退缩；Scenario B 地形上可触发 MISI。

### Phase 2：视觉打磨 + 关卡 2（2 周）

**目标：** 视觉效果达到发布质量；MISI 关卡可玩。

**任务：**
- [ ] 降雪粒子系统
- [ ] 海洋暖流动画
- [ ] 冰山崩解效果
- [ ] 画面震动 + 红色脉冲（崩塌时）
- [ ] 动态解释气泡
- [ ] 关卡 2 "不归之路" 实现
- [ ] 关卡胜负判定逻辑
- [ ] 响应式布局（移动端）

**验收标准：** 视觉效果足以截图分享；关卡 2 中 MISI 触发体验清晰、教学点明确。

### Phase 3：完整关卡 + 分享（2-3 周）

**目标：** 所有关卡可玩；社交分享功能完成。

**任务：**
- [ ] 冰下水文模块实现 (`hydrology.ts`)
- [ ] 关卡 1, 3, 4, 5, 6 实现
- [ ] 隐藏关卡 7 实现
- [ ] 成绩单生成
- [ ] 社交分享（Open Graph + 分享按钮）
- [ ] 海平面→城市影响映射数据
- [ ] 国际化（英/中）

**验收标准：** 所有关卡教学目标明确；分享链接有正确的预览图和描述。

### Phase 4：真实世界 + 发布（2 周）

**目标：** 真实世界模式完成；开源发布。

**任务：**
- [ ] 从 BedMachine 提取 flowline 数据（Wilkes, Thwaites, Totten）
- [ ] 南极选区地图 UI
- [ ] 区域科学背景卡片
- [ ] 性能优化 + 低端设备降级
- [ ] 编写 README + 贡献指南
- [ ] GitHub 开源发布
- [ ] 部署到 Vercel

**验收标准：** 三个真实区域可选择和模拟；README 完整；Lighthouse 性能评分 > 90。

### Phase 5（后续迭代）

- [ ] 与 3D ICE 整合（3D 地球仪 → 点击区域 → 进入 2D 模拟器）
- [ ] 教师面板（管理课堂进度、查看学生结果）
- [ ] 更多真实世界区域
- [ ] 多语言扩展（日语、法语、西班牙语等）
- [ ] 嵌入式模式（iframe，可嵌入新闻文章）

---

## 9. 破圈与运营策略

### 9.1 发布节奏

| 时间节点 | 动作 |
|----------|------|
| Phase 1 完成后 | 发布 teaser GIF 到 Twitter/X（#Antarctica #ClimateChange） |
| Phase 2 完成后 | 邀请 10 位冰川学家内测 → 收集反馈和引用 |
| Phase 4 完成后 | 正式发布：Hacker News, Reddit r/dataisbeautiful, 科学传播社区 |
| 南极新闻事件时 | 快速响应：在社交媒体上发布"用模拟器体验 XXX 正在发生什么" |
| 学期开始前 | 联系高校地理/气候课程教师，提供教案 |

### 9.2 传播渠道优先级

1. **Twitter/X 科学社区：** 冰川学家、气候科学家圈子 → 你已有的合作者网络是启动点
2. **Hacker News / Reddit：** "Show HN" 帖子 → 开发者和技术爱好者
3. **Reddit r/dataisbeautiful、r/climate：** 交叉传播
4. **教育平台：** 联系 NSTA (美国), ASTA (澳洲) 等科学教师协会
5. **科学媒体：** Carbon Brief, The Conversation, 果壳网
6. **中文传播：** 知乎、微博、B站（可录制演示视频）

### 9.3 SEO 与可发现性

- 核心关键词：SL-ICE, ice sheet simulator, Antarctic ice melt interactive, sea level rise game, climate change education
- 每个关卡有独立 URL（可直接分享特定关卡）
- Open Graph 和 Twitter Card meta tags
- JSON-LD 结构化数据（Educational Application schema）

### 9.4 开源社区策略

- MIT 许可证
- 清晰的 CONTRIBUTING.md（欢迎翻译、新关卡、新区域贡献）
- Issue 模板标注 "good first issue"
- 科学家贡献新区域的指南（如何准备 flowline 数据）

---

## 10. 附录：数学公式完整推导

### A.1 坐标变换：物理坐标 → σ 坐标

定义 terrain-following 坐标：
```
σ = (z - b(x)) / H(x),   σ ∈ [0, 1]
```

其中 σ=0 为冰底，σ=1 为冰面。

导数变换：
```
∂/∂x|_z  = ∂/∂x|_σ - (1/H)(∂b/∂x + σ ∂H/∂x) · ∂/∂σ
∂/∂z     = (1/H) · ∂/∂σ
```

### A.2 σ 坐标下的 BP 方程

将 BP 方程变换到 (x, σ) 坐标：

```
∂/∂x(4η ∂u/∂x) - ∂/∂x(4η · α/H · ∂u/∂σ)
- α/H · ∂/∂σ(4η ∂u/∂x) + α²/H² · ∂/∂σ(4η/H · ∂u/∂σ)
+ 1/H² · ∂/∂σ(η ∂u/∂σ)
= ρᵢg ∂s/∂x
```

其中 α(x,σ) = ∂b/∂x + σ · ∂H/∂x。

**简化：** 对于中等坡度的冰盖（典型情况），α/H 项相比 1/H² 项是高阶小量，可以保留主要项：

```
∂/∂x(4η ∂u/∂x) + 1/H² · ∂/∂σ(η ∂u/∂σ) = ρᵢg ∂s/∂x
```

这是 BP 方程在 σ 坐标下的标准简化形式，也是数值实现的基础。

### A.3 有限差分离散

在 (x_i, σ_j) 网格上，令 u_{i,j} = u(x_i, σ_j)：

**x 方向二阶导数（纵向应力项）：**
```
∂/∂x(4η ∂u/∂x)|_{i,j} ≈ [4η_{i+½,j}(u_{i+1,j} - u_{i,j}) - 4η_{i-½,j}(u_{i,j} - u_{i-1,j})] / Δx²
```

**σ 方向二阶导数（垂直剪切项）：**
```
1/H² · ∂/∂σ(η ∂u/∂σ)|_{i,j} ≈ [η_{i,j+½}(u_{i,j+1} - u_{i,j}) - η_{i,j-½}(u_{i,j} - u_{i,j-1})] / (H_i² · Δσ²)
```

组装为线性系统 **A** · **u** = **f**，其中 **A** 是带状矩阵（bandwidth = N_σ）。

### A.4 带状矩阵求解

将 2D 网格 (i, j) 展平为 1D 索引 k = i × N_σ + j。

矩阵 **A** 的结构：
- 主对角线：k 处的系数
- ±1 偏移对角线：σ 方向相邻节点（垂直剪切）
- ±N_σ 偏移对角线：x 方向相邻节点（纵向应力）

总 bandwidth = 2 × N_σ + 1。对于 N_σ = 15，bandwidth = 31。

使用 **LU 分解（带状）** 求解，复杂度为 O(N × bandwidth²)，其中 N = N_x × N_σ。对于典型的 N=1000, bandwidth=31，单次求解 < 1ms。

### A.5 Picard 迭代流程

```
Input:  u⁰ (初始速度猜测), H, b, s, params
Output: u (收敛的速度场)

for k = 1, 2, ..., max_iter:
    1. 计算应变率:
       ε̇_xx = ∂u^{k-1}/∂x
       ε̇_xz = ½ · (1/H) · ∂u^{k-1}/∂σ
       ε̇_e = sqrt(ε̇_xx² + ε̇_xz² + ε̇₀²)

    2. 计算黏度:
       η = ½ · A⁻¹/ⁿ · ε̇_e^{(1-n)/n}

    3. 组装线性系统 A(η) · uᵏ = f

    4. 求解 uᵏ

    5. 检查收敛:
       if ||uᵏ - u^{k-1}|| / ||uᵏ|| < tol (e.g., 10⁻⁴):
           return uᵏ

    6. 可选：松弛
       uᵏ ← ω · uᵏ + (1-ω) · u^{k-1},  ω ∈ [0.5, 1.0]
```

### A.6 半隐式冰厚演化

```
H^{n+1}_i = H^n_i + Δt · [ȧ_i - ḃ_i
    - (F_{i+½} - F_{i-½}) / Δx]
```

其中通量 F 使用上风格式：
```
F_{i+½} = ū_{i+½}^{n+1} · H^*_{i+½}
H^*_{i+½} = H^n_i       if ū_{i+½} > 0
           = H^n_{i+1}   if ū_{i+½} < 0
```

深度平均速度：
```
ū_i = (1/N_σ) · Σ_j u_{i,j}
```

### A.7 接地线子单元参数化

对于包含接地线的网格单元 [x_i, x_{i+1}]：

```
λ = (x_gl - x_i) / (x_{i+1} - x_i)    # 接地比例
```

该单元的底部摩擦按比例混合：
```
τ_b = λ · C · u_b^m · N^q + (1-λ) · 0
    = λ · C · u_b^m · N^q
```

浮力条件插值求 x_gl：
```
f(x) = ρᵢ · H(x) + ρ_w · b(x)
x_gl: f(x_gl) = 0
```

线性插值：
```
x_gl = x_i - f_i · (x_{i+1} - x_i) / (f_{i+1} - f_i)
```

---

*文档结束。本计划书为 "SL-ICE" 项目的完整规格说明，可直接用于 Claude Code 实现。*
